# DailyInsightHub - 知识日报聚合站

一个自动化的知识聚合平台，每日从多个信息源抓取热点内容，通过 AI 进行分类、摘要、翻译，并生成结构化的每日知识日报。

## 功能特性

- **多源数据抓取** — 聚合热榜、RSS、Folo 三大类数据源，覆盖中英文技术与商业内容
- **AI 智能处理** — 自动分类、摘要生成、英文翻译、每日趋势分析
- **两段式入库** — 基础数据先入库保底，AI 增强数据后续更新，确保数据不丢失
- **每日 AI 日报** — 自动聚类热点话题，生成 Markdown 格式的趋势分析报告
- **跨日期去重** — 同一文章只出现在最早被抓取的日期，避免内容重复
- **双视图模式** — AI 日报 + 文章列表自由切换
- **分类筛选** — 8 大内容分类，支持按类别浏览
- **全文搜索** — 支持标题和摘要的模糊搜索（`Ctrl+K`）
- **定时任务** — 支持 Vercel Cron 或手动触发

## 技术栈

| 层级 | 技术 |
|------|------|
| 框架 | Next.js 16 (App Router) |
| 语言 | TypeScript 5.9 |
| 样式 | Tailwind CSS 4 |
| 数据库 | Supabase (PostgreSQL) |
| AI | OpenAI 兼容 API (DeepSeek / Claude 等) |
| 渲染 | React 19 + react-markdown |

## 数据源

### 热榜平台 (NewsNow API)

| 平台 | 语言 | 默认分类 |
|------|------|----------|
| 知乎 | 中文 | 新闻 |
| 微博 | 中文 | 新闻 |
| IT之家 | 中文 | 编程开发 |
| V2EX | 中文 | 编程开发 |
| 少数派 | 中文 | 效率与成长 |
| 掘金 | 中文 | 编程开发 |
| Hacker News | 英文 | 编程开发 |
| Product Hunt | 英文 | 产品设计 |
| GitHub | 英文 | 编程开发 |

> 还支持抖音、B站、百度、今日头条、华尔街见闻等 10+ 平台，可在 `src/config/platforms.ts` 中启用。

### RSS 订阅源

36氪、InfoQ、机器之心、DEV Community、OpenAI Blog、Hugging Face、TechCrunch、Ars Technica、MIT Technology Review

> 可在 `src/config/rss-feeds.ts` 中自定义添加。

### Folo (Follow)

通过 Folo API 获取个人订阅源内容，支持文章和社交短内容。

## AI 处理管道

```
抓取数据 → 批内去重 → 跨日期去重 → AI 分类 → 首次入库(保底)
                                                    ↓
                                    AI 摘要 → AI 翻译 → 二次入库(增强)
                                                    ↓
                                              生成每日 AI 日报
```

| 步骤 | 说明 |
|------|------|
| **分类** | 将文章分到 8 大类别（AI、编程、前端、后端、产品、商业、效率、新闻） |
| **摘要** | 为热榜等无正文的条目生成简短摘要 |
| **翻译** | 英文标题和摘要翻译为中文，保留技术术语 |
| **日报分析** | 聚类相似话题，提炼热点趋势、跨领域关联、值得关注的信号 |

## 内容分类

| 分类 | 图标 | 覆盖范围 |
|------|------|----------|
| AI 与大模型 | 🤖 | AI、LLM、Agent、MLOps |
| 编程开发 | 💻 | 语言、框架、开源项目 |
| 前端与设计 | 🎨 | 前端技术、UI/UX、设计系统 |
| 后端与架构 | ⚙️ | 后端、数据库、系统设计 |
| 产品设计 | 📐 | 产品思维、用户体验、增长 |
| 商业与创业 | 📈 | 商业洞察、创业、投资 |
| 效率与成长 | 🚀 | 工具、方法论、个人成长 |
| 新闻 | 📰 | 行业新闻、公司动态、政策 |

## 快速开始

### 前置条件

- Node.js 18+
- 一个 [Supabase](https://supabase.com) 项目
- 一个 OpenAI 兼容的 AI API Key（DeepSeek、OpenAI、Claude 等）

### 1. 克隆项目

```bash
git clone https://github.com/your-username/knowleage-report.git
cd knowleage-report
npm install
```

### 2. 配置环境变量

复制 `.env.local.example` 或新建 `.env.local`：

```env
# Supabase
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# AI（OpenAI 兼容接口）
AI_API_KEY=your-api-key
AI_MODEL=deepseek/deepseek-chat
AI_API_BASE=https://api.deepseek.com
AI_FALLBACK_MODEL=openai/gpt-4o-mini  # 可选

# Folo（可选，从浏览器 DevTools 获取 Cookie）
FOLO_COOKIE=__Secure-better-auth.session_token=xxx

# 定时任务
CRON_SECRET=your-secret-key
```

### 3. 初始化数据库

在 Supabase Dashboard → SQL Editor 中执行 `supabase-schema.sql`。

### 4. 启动开发服务器

```bash
npm run dev
```

### 5. 触发首次数据抓取

```bash
curl -X POST http://localhost:3000/api/cron \
  -H "x-cron-secret: your-secret-key"
```

也可以指定日期：

```bash
curl -X POST "http://localhost:3000/api/cron?date=2026-02-09" \
  -H "x-cron-secret: your-secret-key"
```

## API 接口

### `POST /api/cron`

触发每日数据管道。

| 参数 | 方式 | 说明 |
|------|------|------|
| `x-cron-secret` | Header | 鉴权密钥（必填） |
| `date` | Query | 指定日期 `YYYY-MM-DD`，默认今天 |

### `GET /api/articles`

获取文章列表。

| 参数 | 说明 | 默认值 |
|------|------|--------|
| `date` | 日期 | 今天 |
| `category` | 分类筛选 | 全部 |
| `search` | 搜索关键词 | - |
| `page` | 页码 | 1 |
| `limit` | 每页数量 | 50（最大 100） |

### `GET /api/report`

获取指定日期的 AI 日报。

| 参数 | 说明 | 默认值 |
|------|------|--------|
| `date` | 日期 | 今天 |

### `GET /api/report-dates`

获取所有有日报的日期列表。

## 项目结构

```
src/
├── app/
│   ├── api/
│   │   ├── cron/           # 定时任务触发端点
│   │   ├── articles/       # 文章列表 API
│   │   ├── report/         # AI 日报 API
│   │   └── report-dates/   # 日期列表 API
│   ├── page.tsx            # 主页面（双视图模式）
│   └── layout.tsx          # 根布局
├── components/
│   ├── article-card.tsx    # 文章卡片
│   ├── daily-report.tsx    # 日报展示
│   ├── category-tabs.tsx   # 分类标签栏
│   ├── date-sidebar.tsx    # 日期导航
│   ├── toc-sidebar.tsx     # 日报目录大纲
│   ├── search-dialog.tsx   # 搜索对话框
│   ├── heat-indicator.tsx  # 热度指示器
│   └── source-badge.tsx    # 来源徽章
├── config/
│   ├── categories.ts       # 分类定义
│   ├── platforms.ts        # 热榜平台配置
│   ├── rss-feeds.ts        # RSS 源配置
│   └── prompts/            # AI 提示词模板
├── lib/
│   ├── ai/                 # AI 处理模块
│   │   ├── classify.ts     # 分类
│   │   ├── summarize.ts    # 摘要
│   │   ├── translate.ts    # 翻译
│   │   ├── analyze.ts      # 日报分析
│   │   └── client.ts       # AI 客户端
│   ├── sources/            # 数据源
│   │   ├── hotlist.ts      # 热榜抓取
│   │   ├── rss.ts          # RSS 解析
│   │   └── folo.ts         # Folo API
│   ├── cron/
│   │   └── runner.ts       # 管道主流程
│   ├── dedup.ts            # 去重逻辑
│   ├── supabase.ts         # 数据库客户端
│   ├── types.ts            # 类型定义
│   └── utils.ts            # 工具函数
└── supabase-schema.sql     # 数据库建表 SQL
```

## 部署

### Vercel 部署

1. 将项目推送到 GitHub
2. 在 [Vercel](https://vercel.com) 导入项目
3. 配置环境变量
4. 设置 Cron Job（`vercel.json`）：

```json
{
  "crons": [
    {
      "path": "/api/cron",
      "schedule": "0 0 * * *"
    }
  ]
}
```

### 自建定时任务

使用 crontab、GitHub Actions 或其他调度器定时调用 `/api/cron` 端点即可。

## 自定义配置

### 添加热榜平台

编辑 `src/config/platforms.ts`，将 `enabled` 设为 `true`：

```typescript
{ id: "bilibili", name: "B站", enabled: true, defaultCategory: "news", language: "zh" },
```

### 添加 RSS 源

编辑 `src/config/rss-feeds.ts`：

```typescript
{
  url: "https://example.com/feed.xml",
  name: "示例站点",
  defaultCategory: "programming",
  language: "zh",
}
```

### 切换 AI 模型

修改 `.env.local` 中的 `AI_MODEL` 和 `AI_API_BASE`，支持任何 OpenAI 兼容接口：

```env
# DeepSeek
AI_MODEL=deepseek/deepseek-chat
AI_API_BASE=https://api.deepseek.com

# OpenAI
AI_MODEL=gpt-4o-mini
AI_API_BASE=https://api.openai.com/v1

# Claude (通过 LiteLLM 等代理)
AI_MODEL=claude-haiku-4-5-20251001
AI_API_BASE=https://your-proxy.com/
```

## 设计决策

| 决策 | 原因 |
|------|------|
| 两段式入库 | 基础数据先入库保底，即使 AI 处理失败也不丢数据 |
| 跨日期去重 | 同一文章只出现在最早日期，避免每日内容高度重复 |
| 批量 AI 处理 | 控制 API 调用频率，降低成本 |
| 优雅降级 | AI 步骤失败不阻塞整个管道，继续后续流程 |
| URL 标准化 | 移除追踪参数、统一格式，提升去重准确率 |
| 标题相似度 | Levenshtein 距离（阈值 0.85），捕获标题微变的重复内容 |

## License

MIT
